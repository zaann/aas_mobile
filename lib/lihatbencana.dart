import 'package:flutter/material.dart';

class LihatBencanaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lihat Bencana'),
      ),
      body: Center(
        child: Text('Isi halaman lihat bencana di sini'),
      ),
    );
  }
}
